---
name: ideation-consultant
description: Placeholder skill contract for product ideation output that feeds PRD authoring and blackboard artifacts.
---

# Ideation Consultant Skill (Placeholder)

## Purpose

Provide a stable integration contract for future ideation automation so that upstream ideation output can be consumed by downstream PRD/TRD/task-planning stages without breaking pipeline contracts.

## Status

This is a placeholder contract. It defines required inputs/outputs and blackboard artifact paths and is intended to be replaced with an implementation generated by `skill-creator`.

## Inputs

1. Product intent or problem statement.
2. Target users and outcomes.
3. Constraints and market context.
4. Pipeline id/slug (`<4digit>-<slug>`) used for artifact naming.

## Output Contract

- Blackboard artifact path:
  - `.blackboard/artifacts/ideation/<4digit>-<slug>.json`
- Schema:
  - `tasks/contracts/blackboard/ideation-output.schema.json`
- Optional narrative artifact:
  - `tasks/ideation-<4digit>-<slug>.md`

## Stage Routing

- Producer stage: `IDEATION`
- Primary consumer stage: `PRD`
- Event routing must remain adjacency-only via existing stage router contracts.

## Safety Rules

1. Do not bypass repository process rules.
2. Do not write PRD/TRD/task artifacts directly from this placeholder contract.
3. Keep outputs schema-compatible for downstream automation.
