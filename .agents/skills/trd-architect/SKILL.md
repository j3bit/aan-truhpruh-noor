---
name: trd-architect
description: Placeholder skill contract for converting PRD and architecture constraints into TRD and TRD blackboard artifacts.
---

# TRD Architect Skill (Placeholder)

## Purpose

Provide a stable integration contract for future TRD authoring automation so downstream task planning can use architecture-complete TRD inputs.

## Status

This is a placeholder contract. It defines required inputs/outputs and blackboard artifact paths and is intended to be replaced with an implementation generated by `skill-creator`.

## Inputs

1. PRD path (`tasks/prd-<4digit>-<slug>.md`).
2. Architecture constraints and repository conventions.
3. Pipeline id/slug (`<4digit>-<slug>`) used for artifact naming.

## Output Contract

- TRD path:
  - `tasks/trd-<4digit>-<slug>.md`
- Blackboard artifact path:
  - `.blackboard/artifacts/trd/<4digit>-<slug>.json`
- Schema:
  - `tasks/contracts/blackboard/trd-output.schema.json`

## Stage Routing

- Producer stage: `TRD`
- Primary consumer stage: `TASK_PLANNING`
- Event routing must remain adjacency-only via existing stage router contracts.

## Safety Rules

1. Keep TRD sections repository-compliant.
2. Do not generate task plans directly from this placeholder.
3. Keep outputs schema-compatible for downstream automation.
